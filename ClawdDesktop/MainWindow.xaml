<Window x:Class="ClawdDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mike PRO | Desktop" Height="600" Width="1000"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip" MouseDown="Window_MouseDown"
        Icon="Resources/AppIcon.ico">
    
    <Window.Resources>
        <Style x:Key="SexyWindowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="bg" Property="Background" Value="#3f3f46"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SexyCloseButton" TargetType="Button" BasedOn="{StaticResource SexyWindowButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ef4444"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SexyComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#0a0a0b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#27272a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <ToggleButton x:Name="ToggleButton" 
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Stretch"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid Background="Transparent">
                                                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,30,0"/>
                                                    <Path Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"
                                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter x:Name="ContentSite" 
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="10,5,30,5"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True">
                                <Border Background="#18181b" 
                                        BorderBrush="#27272a" 
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,5,0,0">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SexyComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#18181b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3b82f6"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2563eb"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" BorderBrush="#3b82f6" BorderThickness="1" Background="#0a0a0b" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="Black" Opacity="0.6" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Background="#18181b" CornerRadius="15,15,0,0" BorderBrush="#27272a" BorderThickness="0,0,0,1">
                <Grid Margin="25,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="35" Height="35" Background="#3b82f6" CornerRadius="8">
                            <TextBlock Text="M" Foreground="White" FontWeight="Black" FontSize="20" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="15,0">
                            <TextBlock Text="MIKE PRO STUDIO" Foreground="White" FontWeight="Black" FontSize="18"/>
                            <TextBlock Text="NATIVE COMMAND CENTER v3.0" Foreground="#60a5fa" FontSize="9" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ToggleButton x:Name="PowerBtn" IsChecked="True" Width="44" Height="22" Click="PowerBtn_Click" Margin="0,0,20,0">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Bg" CornerRadius="11" Background="#10b981">
                                        <Ellipse x:Name="Dot" Width="16" Height="16" Fill="White" HorizontalAlignment="Right" Margin="3,0"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="False">
                                            <Setter TargetName="Bg" Property="Background" Value="#ef4444"/>
                                            <Setter TargetName="Dot" Property="HorizontalAlignment" Value="Left"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <Button Content="—" Style="{StaticResource SexyWindowButton}" Click="Minimize_Click" FontSize="18"/>
                        <Button x:Name="MaxBtn" Content="▢" Style="{StaticResource SexyWindowButton}" Click="Maximize_Click" FontSize="18" Margin="8,0"/>
                        <Button Content="×" Style="{StaticResource SexyCloseButton}" Click="Close_Click" FontSize="22" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- CHAT AREA -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatPanel" Margin="25"/>
            </ScrollViewer>

            <!-- INPUT AREA -->
            <Border Grid.Row="2" Background="#18181b" CornerRadius="0,0,15,15" Padding="20" BorderBrush="#27272a" BorderThickness="0,1,0,0">
                <Grid>
                    <!-- MAIN INPUT -->
                    <Grid x:Name="InputGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="📁" Background="Transparent" BorderThickness="0" Foreground="#3b82f6" FontSize="22" Click="Upload_Click"/>
                        
                        <TextBox x:Name="InputBox" Grid.Column="1" Background="#0a0a0b" Foreground="White" BorderBrush="#27272a" 
                                 Padding="15,12" Margin="15,0" BorderThickness="1" KeyDown="InputBox_KeyDown"
                                 VerticalContentAlignment="Center" CaretBrush="#3b82f6" FontSize="15">
                        </TextBox>
                        
                        <ComboBox x:Name="TranscriptionProvider" Grid.Column="2" Width="100" Margin="0,0,10,0" SelectedIndex="0" 
                                  VerticalContentAlignment="Center" Style="{StaticResource SexyComboBox}">
                            <ComboBoxItem Content="Local" Style="{StaticResource SexyComboBoxItem}"/>
                            <ComboBoxItem Content="OpenAI" Style="{StaticResource SexyComboBoxItem}"/>
                            <ComboBoxItem Content="Windows" Style="{StaticResource SexyComboBoxItem}"/>
                        </ComboBox>
                        
                        <PasswordBox x:Name="ApiKeyBox" Grid.Column="3" Width="150" Margin="0,0,10,0" Background="#0a0a0b" Foreground="White" 
                                    BorderBrush="#27272a" Padding="10,8" VerticalContentAlignment="Center" 
                                    ToolTip="OpenAI API Key (required for OpenAI provider)"/>
                        
                        <Button Grid.Column="4" x:Name="MicBtn" Content="🎤" Background="Transparent" BorderThickness="0" Foreground="#ef4444" FontSize="24" Click="Mic_Click"/>
                    </Grid>

                    <!-- WHATSAPP RECORDING OVERLAY (RIGHT ALIGNED) -->
                    <Border x:Name="RecordingOverlay" Grid.Column="0" Grid.ColumnSpan="5" Background="#18181b" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- VOICE WAVEFORM -->
                            <Canvas x:Name="WaveCanvas" Grid.Column="0" Height="40" Margin="20,0">
                                <Polyline x:Name="WaveLine" Stroke="#ef4444" StrokeThickness="2" Points="0,20 10,20 20,20 30,20 40,20 50,20 60,20 70,20 80,20 90,20 100,20"/>
                            </Canvas>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                                <Ellipse x:Name="RecDot" Width="10" Height="10" Fill="#ef4444" Margin="0,0,10,0"/>
                                <TextBlock x:Name="TimerText" Text="0:00" Foreground="White" FontSize="18" FontWeight="Bold" FontFamily="Consolas"/>
                            </StackPanel>
                            
                             <Button Grid.Column="2" Content="CANCEL" Background="#ef4444" Foreground="White" Padding="25,10" BorderThickness="0" FontWeight="Bold" Click="CancelRecording_Click" Margin="0,0,10,0"/>
                             <Button Grid.Column="3" Content="DONE" Background="#10b981" Foreground="White" Padding="25,10" BorderThickness="0" FontWeight="Bold" Click="Mic_Click"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
